package com.autoya.autoya_api.iam.domain.model.commands;

/**
 * Record representing a command for seeding roles in the IAM (Identity and Access Management) domain.
 * This record serves as a lightweight data structure for initiating role seeding operations.
 */
public record SeedRolesCommands() {
}

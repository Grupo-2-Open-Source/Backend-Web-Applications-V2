package com.autoya.autoya_api.iam.domain.model.commands;

/**
 * Record representing a command for user sign-in in the IAM (Identity and Access Management) domain.
 * This record serves as a lightweight data structure for initiating user sign-in operations.
 */
public record SingInCommands() {
}

package com.autoya.autoya_api.iam.domain.model.valueobjects;

/**
 * Enumeration representing roles in the IAM (Identity and Access Management) domain.
 * This enum defines various roles that users can have within the system.
 */
public enum Roles {
    ROLE_TENANT,
    ROLE_OWNER,
    ADMIN_ROLE,
}
